pipeline {
    agent any 
    stages {
        stage('git checkout') { 
            steps {
               echo "cloning"
                git 'https://github.com/said1024/jenkins3.git'
            }
        }
 stage('maven build') { 
            steps {
               echo "build"
                sh "clean package"
            }
        }
       
    }
}
