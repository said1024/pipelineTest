pipeline {
    agent any 
    stages {
        stage('git checkout') { 
            steps {
               echo "cloning"
                git 'https://github.com/said1024/jenkins3.git'
            }
        }
       
    }
}
